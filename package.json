{
  "name": "alek-portfolio",
  "private": true,
  "packageManager": "pnpm@10.12.3",
  "scripts": {
    "dev": "pnpm --filter @repo/shared build && pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  }
}
